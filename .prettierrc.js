module.exports = {
    printWidth: 180, // Maximum line width
    tabWidth: 4, // Number of spaces for indentation
    useTabs: false, // Use spaces instead of tabs for indentation
    semi: true, // Add semicolons at the end of statements
    singleQuote: true, // Use single quotes instead of double quotes
    arrowParens: 'avoid', // Omit parentheses when arrow function has a single parameter
    bracketSpacing: true, // Add spaces between brackets in object literals, e.g. "{ foo: bar }"
    htmlWhitespaceSensitivity: 'ignore',
    stylelintIntegration: false, // Don't let prettier use stylelint code format for validation
    trailingComma: 'es5', // Add trailing commas for objects and arrays in ES5
    tslintIntegration: false, // Don't let prettier use tslint code format for validation
};
